version: '3.9'
services:
    bot:
        build:
            context: ''
            dockerfile: docker/Dockerfile
        container_name: ithappens_bot
        restart: always
        networks:
          - vpc

networks:
  vpc:
    name: ithappens-bot-network
